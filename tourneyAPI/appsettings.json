{
  "AllowedHosts": "*",

  "//Email": [
    "Leave Smtp.Host empty and no mail is sent: confirmation and reset links are",
    "written to the log instead, with a warning saying so. That is the intended",
    "state for a developer machine, and it keeps sign-up usable locally now that",
    "a confirmed address is required to sign in.",
    "",
    "Do NOT put a password in this file. It is committed. Supply secrets out of",
    "band, as environment variables:",
    "  setx Email__Smtp__UserName \"...\"",
    "  setx Email__Smtp__Password \"...\"",
    "or, for local development, with user secrets:",
    "  dotnet user-secrets set \"Email:Smtp:Password\" \"...\" --project tourneyAPI",
    "",
    "PublicBaseUrl pins the host used in emailed links. Leave it empty to let",
    "Identity build them from the incoming request; set it when the app sits",
    "behind a proxy or a tunnel, where the request host is not something a mail",
    "client can reach."
  ],
  "Email": {
    "FromAddress": "",
    "FromName": "Smash Tourney",
    "PublicBaseUrl": "",
    "Smtp": {
      "Host": "",
      "Port": 587,
      "UseStartTls": true,
      "UserName": "",
      "Password": "",
      "TimeoutSeconds": 10
    }
  }
}
