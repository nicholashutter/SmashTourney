#Test Coverage Reports
CoverageReport/

# .NET build artifacts
bin/
obj/
/obj/
/bin/
*.user
*.rsuser
*.suo
*.ncrunchsolution
*.ncrunchproject
*.vspscc
*.vssscc

API_Docs/

tourneyAPI/obj
Servies/obj
# EF migrations are source, not build output, and are deliberately tracked.
# They are the record of how the schema got to its current shape, and startup
# applies them — ignoring them means a clean clone cannot build its database.
# Only the packaged bundle executable stays out.
*.efbundle

# Rider IDE files
.idea/

# Visual Studio Code settings
**/.vscode/*
!.vscode/
!.vscode/launch.json
!.vscode/tasks.json

# NuGet dependencies
*.nupkg
packages/
project.lock.json
project.assets.json

# User-specific files
*.swp
*.swn
*.bak
Thumbs.db

#logs
logs/

#pre populated db files
*.db
*.db**
